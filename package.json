{
  "name": "presentable-web",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "mkdir -p dist; browserify index.js -o dist/presentable-web.dist.js -t [ babelify --presets env ] -t uglifyify --standalone present"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "babel-preset-env": "^1.3.2",
    "babelify": "^10.0.0",
    "browserify": "^16.2.2",
    "uglifyify": "^5.0.1"
  }
}
