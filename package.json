{
  "name": "presentable-web",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "mkdir -p dist; browserify index.js -o dist/presentable-web.dist.js -t [ babelify --presets env ] -t uglifyify"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "babel-preset-env": "^1.3.2",
    "babelify": "^7.3.0",
    "browserify": "^14.1.0",
    "uglifyify": "^3.0.4"
  }
}
